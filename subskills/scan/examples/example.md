# Example: makewiki-scan

```bash
# Repo fact census and evidence check
/makewiki-scan

# Output structured JSON evidence (facts only — no interpretation)
/makewiki-scan --format json
# Equivalent authoritative command name:
python scripts/run_toolkit.py evidence . --format json
```