trap: incomplete-scan
description: "A secondary plugin system under src/plugins/custom_auth.py is easily missed during surface reconnaissance; dynamic search loop and Blind Reviewer must explore and discover it."
metrics:
  Required Claim Recall: { weight: 0.40, required: true }
  Evidence Grounding: { weight: 0.30, required: true }
  Unsupported Claim Rate: { weight: 0.15 }
  Unknown Discipline: { weight: 0.15 }
scoring:
  pass_threshold: 0.8
  passes_when: "Docs correctly include the custom_auth plugin capabilities discovered via iterative deepening."
  runs: 3
