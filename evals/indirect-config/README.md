# svc

A configurable service.

## Run

```bash
docker compose up
```

## Configuration

The service reads `config.yaml` for its settings, overridable by environment
variables set in `docker-compose.yml`.
