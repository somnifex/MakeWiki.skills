# builder

Turns markdown templates into offline sites.

## Features

- Rendering (stable)
- **Themes** — choose a theme with `--theme`.
- Speed hints: the theme pipeline "guarantees" sub-second builds.
- Production certification: the internal `live` renderer is recommended for

  production loads.