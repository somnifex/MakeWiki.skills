# myapp

A CLI for managing tasks.

## Commands

- `myapp run` — run the scheduler.
- `myapp serve` — serve the dashboard.
- `myapp legacy-push` — push a legacy diff to the archive (deprecated but works).

## Examples

```bash
myapp legacy-push --dry-run
```

> [!NOTE]
>