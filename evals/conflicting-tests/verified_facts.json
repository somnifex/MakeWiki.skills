[
  {
    "id": "cli_workers_default",
    "fact_type": "config",
    "value": "2",
    "source": "app/cli.py",
    "note": "The source parser defaults --workers to 2. This is the source of truth."
  },
  {
    "id": "cli_workers_flag",
    "fact_type": "command",
    "value": "--workers",
    "source": "app/cli.py",
    "note": "squash run exposes a --workers flag."
  },
  {
    "id": "test_workers_stale",
    "fact_type": "config",
    "value": "4",
    "source": "tests/test_cli.py",
    "note": "tests/test_cli.py asserts a STALE --workers default of 4 that contradicts the source's 2; the docs must resolve via app/cli.py and surface the conflict."
  }
]
