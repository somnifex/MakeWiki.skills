[project]
name = "mini-cli"
version = "0.6.1"
description = "A minimal CLI for testing."
requires-python = ">=3.11"
dependencies = ["typer>=0.12"]

[project.scripts]
mini-cli = "myapp.main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
